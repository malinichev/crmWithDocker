import { CyHttpMessages } from 'cypress/types/net-stubbing';

export const createFixture = (req: CyHttpMessages.IncomingHttpRequest) => {
    // const fixtureName = req.method + req.url;
    // создаем файлик JSON с нужными данными
    // createJson(fixtureName, req.body);
};
